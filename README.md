# OpenGL Project

## About
A simple unfinished OpenGL project.

## Current Status
- Cube is not rendering for some reason  
- Plane is also not rendering  
- I’ll debug it later when I feel like it 😄  

## Notes
This is a very basic OpenGL setup. No advanced or fancy features are included yet, but I may expand it in the future.

## ⚠️ Important Warning
- Do NOT use the provided `CMakeLists.txt` file in this repository.  
- It is broken and unfinished, and may not build the project correctly.

## ⚠️ Important Notes
- Do NOT modify or delete any generated project files.
- Do NOT touch or edit the generated project files in any way, as they are required for the build system to work properly.

- Also, do NOT run or modify the `clean.bat` file unless you fully understand what it does. It is used for cleaning build outputs and may remove important generated files if misused.

## Assets
Some 3D models are included for testing purposes (`.obj` ` .fbx`).